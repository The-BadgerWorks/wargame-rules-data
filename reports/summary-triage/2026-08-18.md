<!-- AI-Assisted: Claude Code (model: claude-sonnet-5) - Generated for the wh40k-11e-2026-08-4
     candidate's SUM-NEEDS-REREVIEW triage (Product Owner decision 2026-08-18): the 24 approved
     ability summaries whose live source digest no longer matched their approved digest, each
     ephemerally compared against the current source mechanic via the pipeline's own acquisition
     path, then classified and either resolved or drafted. No source sentence appears below --
     only ability keys, classification reasons, and this session's own original mechanics-only
     drafted prose. Neither the mechanic-digest key nor any digest value is repeated here. -->
# Ability-summary re-review triage — wh40k-11e-2026-08-4

Triggered by the `wh40k-11e-2026-08-4` candidate dispatch (2026-08-18), which failed (exit 30)
with 24 `SUM-NEEDS-REREVIEW` findings plus the derived `COV-SUMMARY-REGRESSION`. All 24 are
Faction-type "Army Rule" abilities — the once-per-battle-round rule every faction publishes one
of. None are option- or equipment-related; unrelated to `008-wargear-option-completion`'s own
work in this same candidate.

**Process**: for each key, the current source mechanic was read once, ephemerally, through the
pipeline's own acquisition path (`pipeline.acquire.detail_source`, the live sitemap union the
declared carry-forward set — the same acquisition every build already performs), compared
against the existing **approved** summary's own mechanics (not wording), and the raw text
discarded immediately after classification. Nothing beyond this report and the two curation file
changes it describes survives that comparison.

## Class (a) — MECHANIC UNCHANGED: 23 keys, dated resolutions written

In every one of these 23 cases, the live source page now carries an appended Errata and/or FAQ
block that was not present when the summary was last approved — which is what moved the
`mechanic_digest` — but the **operative rule** the approved summary describes is unchanged. Nine
of the 23 additionally carry an Errata rewording that restates the same operative text near-
verbatim (no value changed); the remaining 14 carry only a newly-appended FAQ clarifying an edge
case the approved summary was never required to state.

Written to `curation/resolutions.json` (23 new entries, `finding_code: SUM-NEEDS-REREVIEW`, each
digest-bound so the resolution automatically lapses if this key's mechanic ever moves again):

| Ability key | Faction file |
|---|---|
| `faction:acts-of-faith` | f-adepta-sororitas.json |
| `faction:battle-focus` | f-aeldari.json |
| `faction:blessings-of-khorne` | f-chaos-space-marines.json |
| `faction:cabal-of-sorcerers` | f-thousand-sons.json |
| `faction:cabal-of-sorcerers-1` | f-thousand-sons.json |
| `faction:cabal-of-sorcerers-aspiring-sorcerer-only` | f-chaos-space-marines.json |
| `faction:cabal-of-sorcerers-scarab-occult-sorcerer-only` | f-thousand-sons.json |
| `faction:code-chivalric` | f-imperial-knights.json |
| `faction:cult-ambush` | f-genestealer-cults.json |
| `faction:dark-pacts` | f-chaos-daemons.json |
| `faction:doctrina-imperatives` | f-adeptus-mechanicus.json |
| `faction:for-the-greater-good` | f-tau-empire.json |
| `faction:gate-of-infinity` | f-grey-knights.json |
| `faction:harbingers-of-dread` | f-chaos-knights.json |
| `faction:nurgles-gift-aura` | f-chaos-space-marines.json |
| `faction:reanimation-protocols` | f-necrons.json |
| `faction:shadow-in-the-warp` | f-genestealer-cults.json |
| `faction:shadow-in-the-warp-ravener-prime-only` | f-genestealer-cults.json |
| `faction:synapse` | f-genestealer-cults.json |
| `faction:templar-vows` | f-space-marines.json |
| `faction:the-shadow-of-chaos` | f-chaos-daemons.json |
| `faction:voice-of-command` | f-astra-militarum.json |
| `faction:waaagh` | f-orks.json |

Each entry's `explanation` (identical text across all 23 — the same process fact applies to
every one): *"Source page now appends an Errata/FAQ block to this ability's text (published
since this summary was approved), moving the mechanic_digest. The operative rule itself is
unchanged from the approved summary — confirmed by an ephemeral structural comparison, source
text discarded, before this resolution was written."*

`resolved_by` is deliberately written as the placeholder `pending-po-confirmation` — **the
Product Owner must overwrite this with a real identity (or confirm the placeholder stands) before
these resolutions are treated as final**, per this session's own brief ("resolved_by left for the
PO to confirm").

## Class (b) — MECHANIC CHANGED: 1 key, drafted replacement

**`faction:oath-of-moment`** (`f-space-marines.json`). The current source's Errata section
narrows the chapter-exclusion list on Oath of Moment's second effect (the +1 Wound roll bonus for
a Codex: Space Marines Detachment fielding no chapter-specific units): one previously-excluded
chapter is no longer excluded. The approved summary's generalised phrasing ("without
Chapter-specific units") does not name which chapters, so it is not literally false, but it is no
longer *precise* — a reader of a chapter now included in the bonus would be misled about which
chapters the exclusion still names. Classified MECHANIC CHANGED rather than unchanged out of
caution: precision here affects a real game outcome, not merely wording.

**Action taken**: `curation/abilities/f-space-marines.json`'s entry updated —
`review_state` set to `draft` (not `approved`; self-approval is structurally impossible from
`draft`), `mechanic_digest` updated to the current value, `reviewed_by`/`reviewed_at` cleared
(the prior approval no longer applies to new text), `authored_against_acquisition` recorded.

**Drafted summary** (original mechanics-only prose, 340 of 1000 characters, house style):

> At the start of your Command phase, select an enemy unit as your Oath of Moment target until
> your next Command phase. Your models with this ability re-roll the Hit roll against it; if your
> detachment is a Codex: Space Marines Detachment fielding no Blood Angels, Dark Angels,
> Deathwatch, or Space Wolves units, also add 1 to the Wound roll.

**This is a draft, not an approval.** It stays blocking (`SUM-UNAPPROVED` in place of
`SUM-NEEDS-REREVIEW` on the next build) until the Product Owner reviews it against the source
card and flips `review_state` to `approved` themselves — the self-approval guard
(`tools/check_summary_approvals.py`) refuses a PR where the reviewer and the approving actor are
the same identity, and this session is neither.

## What the Product Owner must do before re-dispatch

1. **Review the 23 resolutions above** (`curation/resolutions.json`) and confirm or replace
   `resolved_by: "pending-po-confirmation"` with a real identity on each.
2. **Review the 1 drafted summary** (`faction:oath-of-moment`,
   `curation/abilities/f-space-marines.json`) against the live source card, and either approve it
   (flip `review_state` to `approved`, set `reviewed_by`/`reviewed_at`) or send it back for a
   different draft.
3. Once both are done, re-dispatch `candidate.yml` with `rules_version_id=wh40k-11e-2026-08-4`
   (the same id — this is a continuation of the same candidate attempt, not a new version).

## Gate evidence

`ruff check` clean, `ruff format --check` clean (317 files), `mypy pipeline --strict` clean (96
files), `pytest -q` 2058 passed / 9 skipped, `pytest tests/ip -q` 83 passed — all unchanged from
before this triage, confirming no source text or digest leaked into any committed file.
`tools/check_change_classes.py`: `curation/` only, one class.
