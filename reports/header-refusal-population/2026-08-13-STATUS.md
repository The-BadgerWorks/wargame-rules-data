<!-- AI-Assisted: Claude Code (model: claude-sonnet-5) - Status note for 007 T004, recording why
     the live/cached-corpus run this task names was not performed in this session and exactly how
     to run it. Not the measurement itself — no corpus was read to produce this file. -->
# T004 status: PENDING — live/cached corpus run not yet performed

**The R-2 probe tool (`tools/header_refusal_population.py`) is built, tested, and ready** (14 unit
tests passing against synthetic composition-row skeletons and the offline `fixtures/minimal` set —
see `tests/unit/test_header_refusal_population.py`). Its pure `classify_datasheet()` function is
independently proven correct on invented shapes: a two-row and a three-row summing header, a
header-shaped row whose successors do **not** sum (the exact R-2 population), and the
successor-unresolved case excluded from both buckets. **This file records that T004's own run —
feeding it the live or a legitimately cached current-edition corpus — was not performed in this
session**, and exactly how a session with the right access should perform it.

## Why this session could not run it

T004 is marked **[EXTERNAL ACTION]** in `tasks.md`'s own human-gate table for exactly this reason.
The same three constraints recorded in this session's identical T003 status note
(`reports/footnote-restriction-taxonomy/2026-08-13-STATUS.md`) apply here without change: this
session's operating instructions prohibit initiating any fetch against the live source;
`WGC_DETAIL_SOURCE_URL` is unset in this checkout's environment; and no cached corpus exists
anywhere in this repository to substitute for a live run, by the constitution's own design (raw
source is never committed).

## Exactly how to run it, when access exists

```bash
python tools/header_refusal_population.py
```

No `--fixtures`/`--offline` flags. Produces `reports/header-refusal-population/<date>.md` (note:
without the `-STATUS` suffix this file carries, to distinguish a real measurement from this
placeholder) in the format this tool's own `render()` already emits and this session's smoke test
against `fixtures/minimal` already exercised end to end (9 datasheets with composition, 0 header
candidates in that particular fixture set — it has no phantom-header shape, so the smoke test
proves the code path runs cleanly rather than exercising a populated report).

## What the resulting report answers

Over every datasheet's first composition row that is fixed-count (`min_count == max_count`) with
at least one successor row — a header *candidate*, signals 1-3 of research D1's five-signal rule —
whether its count equals the sum of its successors' maxima (signal 4). Per D8's R-2: `non_sums ==
0` confirms the safe direction holds — every header-shaped row this corpus contains is also a total
of its successors, so T030's signal 4 does not under-refuse a genuine phantom row it should catch.
A non-zero count is not itself evidence of a defect (R-2 already accepts under-refusal, never
over-refusal, as the correct failure direction for a rule that removes published data) — it sizes
how large that residual is, by datasheet id, for review.

**This determination directly sizes signal 4 of T029/T030 (Phase 4, User Story 4) before the rule
is fixed.** Whoever performs T004 for real should read its own report before that task starts,
exactly as `tasks.md` requires.
