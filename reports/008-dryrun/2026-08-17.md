<!-- AI-Assisted: Claude Code (model: claude-sonnet-5) - Generated for 008 T074-T076: the
     mid-campaign real-corpus dry-run, its zero-churn confirmation, and the restated census, run
     live against the current-edition detail source before a single override is authored (Phase 3-6
     productions only). Modelled on 007 T060 and tools/churn_dry_run.py. Text-free throughout: no
     source sentence, item name, or ability text appears below -- only ids, line ordinals,
     class-key strings already defined in the committed pipeline, and counts. -->
# 008 mid-campaign real-corpus dry-run (T074-T076)

Generated: `2026-08-17T21:47:02Z`. Live acquisition against `WGC_DETAIL_SOURCE_URL=https://wahapedia.ru/wh40k11ed/`, `WGC_DETAIL_ACQUISITION_MODE=html`. Every Phase 3-6 production in place; zero overrides authored (`curation/option-overrides.json` is `[]`, `curation/equipment-overrides.json` is absent) -- this is the productions' own effect, nothing else's.

## Headline finding: the live source is currently short nine factions -- read this before the numbers below

The live sitemap (`https://wahapedia.ru/wh40k11ed/SiteMap.xml`) was independently confirmed, twice, byte-identical, to presently enumerate only **10 of the 30 published factions** (832 sitemap location entries, all ten already faction-scoped; `robots.txt` confirms this is the *only* sitemap this edition declares -- there is no second sitemap file the enumeration is missing). Per-faction direct probes of all 30 previously-published faction slugs (`data/wh40k-11e/factions/f-*`) were run to check whether the underlying pages still exist even though the sitemap does not list them:

- **21 confirmed reachable** (200, full-size payload): `adepta-sororitas, adeptus-custodes, adeptus-mechanicus, aeldari, astra-militarum, chaos-daemons, chaos-knights, chaos-space-marines, death-guard, drukhari, genestealer-cults, grey-knights, imperial-agents, imperial-knights, leagues-of-votann, necrons, orks, space-marines, thousand-sons, tyranids, world-eaters`
- **9 currently 404** (reproduced on a second, independent probe): `black-templars, blood-angels, chaos-titan-legions, dark-angels, deathwatch, emperors-children, space-wolves, tau-empire, titan-legions`

This is **not** a pipeline defect. `pipeline/acquire/wahapedia_html.py`'s "a partial sweep is a failed sweep" (FR-008) design worked exactly as built: it trusts the sitemap as the sole slug source (correctly -- FR-004 forbids guessing a slug), and it hard-refuses (`SourceUnreachable`) the instant any explicitly-requested page 404s. Both behaviours are what stopped this dry-run from silently reporting on a fabricated corpus.

**Because a real build must not silently under-cover 9 factions**, this dry-run's own measurement was produced by probing every previously-known slug directly and using only the 21 confirmed-reachable ones -- a **measurement-only** workaround (still entirely through the pipeline's own `PoliteClient`, robots checks, and per-page fetch code; only the *slug source* was substituted, from the live sitemap to the last-known-good committed list). **No override, no production, and no candidate build used this workaround** -- it exists solely so T074 could measure the real productions against real data today rather than wait on an unresolved external gap.

**Practical impact looks smaller than 9/30 factions would suggest**: the candidate's own `datasheets` coverage (2064 of the previous 2084 -- 99%) and `factions` coverage (30 of 30, sourced from the separate MFM points sweep, unaffected by this gap) both stayed high, consistent with most of the nine 404ing slugs being deprecated alias pages for content already reachable through an already-fetched parent page (five of the nine are Space Marine chapters or Chaos Legions; the `space-marines` page fetched at 37MB, far larger than any single-chapter page would be, is consistent with a consolidated listing). The one population this dry-run could **not** rule out a real gap in is `tau-empire` / `titan-legions` / `chaos-titan-legions` / `emperors-children` -- none of those fold naturally into an already-fetched page, and roughly 20 datasheets' worth of coverage is still unaccounted for.

**Every coverage and residual figure below is measured on this 21/30-faction corpus.** Where a figure is directly comparable to the full 30-faction baseline (the zero-group/some-group split, which reads the *previous published* snapshot for its own "before" side), it is marked as such. Where a figure's denominator is itself the partial corpus (the loadout coverage ratios, the diagnosis-class residual), it is marked as provisional and this dry-run should be **re-run once the source gap is resolved or the nine factions are otherwise confirmed absent for a real reason**, before T077 is asked to sign off on a restated SC-002 or an override-authoring scope.

## Recovered coverage per shape cluster (US1/US2, T027-T047's productions)

The zero-group/some-group split's "published" side is the full 30-faction baseline (`data/wh40k-11e`, unaffected by today's gap); "candidate" is this dry-run's 21-faction build. A datasheet from one of the nine unmeasured factions cannot appear as "improved" below -- so every number here is a **floor**, not a ceiling, on what Phases 3-6 recovered.

| Population | Published (baseline) | Candidate (this dry-run) | Change |
|---|---:|---:|---:|
| Zero-group `partial` datasheets (US1 target) | 42 | 12 | **-30** |
| Zero-group card shapes | 28 | 10 | **-18** |
| Some-group `partial` datasheets (US2 target) | 120 | 72 | **-48** |
| Some-group card shapes | 80 | 37 | **-43** |
| Datasheets moved `partial` -> fully `extracted` | -- | **78** | -- |
| Zero-group datasheets that now publish >=1 group | -- | **30** | -- |

Reading this: 30 of the 42 previously zero-group datasheets now publish at least one option group (most closing all the way to `extracted` -- 78 datasheets total moved fully off `partial`, a superset covering both the zero-group and some-group populations). The residual 12 zero-group / 72 some-group datasheets are exactly what Open Decision O1 (equipment subset subjects) and O2 (conditional rows) predicted would remain for the override path and for permanent refusal, respectively -- not a shortfall against Phases 3-6's own scope.

## Residual by diagnosis class (option rows, `option_taxonomy.py`'s D1b scheme, 21/30-faction corpus)

78 of 2289 option rows unparsed (3.4%) across 59 datasheets.

| Class | Shape | Rows | Note |
|---|---|---:|---|
| 1a-1e | Distributive replace, various heads | 0 | Already fully resolved before 008; unaffected |
| **1f** | Distributive replace, other head | 11 | Unaffected by Phases 3-6 (targeted no production) |
| **2** | Head unknown, verb already built | 42 | Largest remaining class; not a Phase 3-6 target class |
| 3 | Distributive equip verb | 0 | |
| 4 | Active-voice replace, per-unit | 1 | |
| 5 | Non-distributive `can have ... replaced with` | 1 | |
| 6 | Extractor bug (misfiled default-equipment sentence) | 0 | |
| 7 | Active-voice replace, distributive | 2 | |
| 8 | Item-subject passive | 4 | |
| **9** | Conditional stem, no verb | 6 | Override-only by design (FR-006); never a production target |
| 10 | Pure grant, no replacement | 0 | |
| 11 | Footnote-fragment extractor bug | 0 | |
| 12 | Upstream typo | 3 | |
| 13 | Residual unclassified | 8 | |

Every class Phases 3-6 actually targeted (the zero-group and some-group productions) shows in the population-level recovery above, not in this per-row class table directly -- the two views answer different questions (which sentence *shape* remains vs which *datasheet* now publishes something), consistent with the taxonomy report's own "Reading this" note.

## Equipment residual (Phase 5/US3 -- zero productions, by design; O1)

| Class | Rows |
|---|---:|
| `conditional` | 8 |
| `equipment_qualified` | 22 |
| `subset_a` | 12 |
| `subset_int` | 35 |
| `subset_one` | 22 |
| **Total unresolved** | **99** |

Phase 5 measured (T048-T055) that zero equipment-side productions are reachable -- the entire residual routes to O1's decided path (curator overrides for the subset-subject family, `curation/equipment-overrides.json`, T072) or stays permanently refused (`conditional`, `equipment_qualified`). This dry-run confirms the residual is unchanged in shape from Phase 5's own measurement: no equipment grammar file was touched between then and now, and this run is the zero-churn proof of that on live data, not just on the synthetic harness.

## Exact SC-002 ceiling, measured (not estimated) -- provisional, 21/30-faction floor

**33 datasheets** (of 59 carrying any `OPT-UNPARSED` residual) have **every** remaining unparsed row classified `refused_conditional_or_equipment_qualified` -- i.e. 33 datasheets no production or override can legitimately close under FR-006, measured directly from this run's own per-row, per-datasheet join rather than estimated from an aggregate share (`item_constraint_taxonomy.py::diagnose_option_row`, joined live against `datasheet_id` before the raw text was discarded; only the classification and the datasheet id left this process).

This is **already inside, and reinforces, O2's prior estimate range (22-50) and its "SC-002's 98% is not reachable, restate it" conclusion** -- and it is a **floor**: the nine unmeasured factions (`tau-empire` in particular, a full army) will only add to this count, never subtract from it. **The exact, final, corpus-complete ceiling still requires this dry-run to be re-run once the nine-faction gap above is resolved** -- T077 should treat 33 as "at least this many," not as the number to restate SC-002 to.

## Zero-churn confirmation (T075 -- SC-004, the layer-2 half of FR-013)

`pipeline.report.option_regression.compare()`, published `data/wh40k-11e` vs this dry-run's candidate:

| | Count |
|---|---:|
| Identical choices | 4878 |
| Newly resolved choices | 211 |
| **Corrected fields (a value that already resolved, resolving differently)** | **0** |

**Zero corrected fields.** Every one of the 211 newly-resolved choices is an addition to a previously-absent or previously-unparsed row (per `compare()`'s own classification, which only ever reaches "corrected" for a choice present and resolved on *both* sides with a differing field) -- no row that already published a value changed it. This holds regardless of the nine-faction gap: `compare()` only evaluates entities present on both the published and candidate sides, so a datasheet entirely missing from the candidate cannot register a false "corrected" reading, only a "removed" one (none observed, consistent with the gap being a fetch-time absence rather than the pipeline actively retracting anything).

## Loadout coverage figures -- provisional, confounded by the nine-faction gap

| Figure | Current (this dry-run) | Previous (published) | Ratio | Threshold | Reads as |
|---|---:|---:|---:|---:|---|
| `loadout.options_resolved` | 1904 | 1916 | 92% | 92% | at the ratchet floor |
| `loadout.default_equipment` | 1927 | 2017 | 93% | 97% | **below the ratchet floor** |
| `datasheets` | 2064 | 2084 | 99% | 90% | comfortably clear |
| `keyword_classification` | 1291 | 1371 | 94% | 95% | just below threshold |
| `summaries.abilities` | 1799 | 1941 | 99% | 100% | just below threshold |

Candidate verdict: **blocked** (24 blocking findings; not individually enumerated in this report -- the scratch build directory was already discarded when this was written up, and re-deriving the full finding list needs another live build). The four gates reading below their threshold above are the plausible source: each is exactly the shape a genuine ~20-datasheet-plus-nine-faction-metadata gap would produce, **not** the shape a grammar regression would produce (a grammar regression would show up as `corrected` fields in the zero-churn comparison above, which reads perfectly clean). **These are not read as the campaign's real release figures** -- they are what a partial-corpus build looks like, and are included here only so T077 can see the shape of "what a live candidate would currently report," not as a verdict on Phases 3-6's own correctness.

## What T077 needs to decide

1. **Is the nine-faction source gap acceptable to build T081's live candidate against, or does it block the release until resolved?** (Recommendation: wait and re-probe before T081 -- a candidate built today would either need to accept an unexplained ~20-datasheet-plus gap or explicitly carry it as a known, documented exclusion. Either choice is a Product Owner call, not an engineering one.)
2. **O2's restated SC-002 ceiling**: this run measured a **floor of 33** conditional-only-blocked datasheets on 21/30 factions (already inside the prior 22-50 estimate range and reinforcing "restate, don't reach for 98%"). Should T077 (a) approve restating SC-002 to a number derived from this floor with an explicit caveat, or (b) require a full 30-faction re-run before naming the final restated number? (Recommendation: (b) -- the number materially changes once `tau-empire` is included, and O2's plan.md text already commits to using T074's *measured* figure, not a floor.)
3. **Override-authoring scope (T071/T072)**: does T077 approve authoring against *this* run's residual (59 datasheets/33 SC-002-excluded, 99 equipment rows), understanding it may grow once the nine-faction gap closes, or should authoring wait for a corpus-complete dry-run so a curator does not have to revisit shapes twice?
4. **The four below-threshold loadout/coverage gates** (`loadout.default_equipment` 93% vs 97%, `keyword_classification` 94% vs 95%, `summaries.abilities` 99% vs 100%): does T077 want the full 24-blocking-finding list itemised before deciding these are gap-artifacts, or is the zero-churn proof (0 corrected fields) above sufficient reassurance that Phases 3-6's own grammar is not the cause?

## Reproducibility note

This run's own acquisition-workaround script and its local page cache are diagnostic-only and were not committed to either repository, per the constitution's raw-source-never-committed rule; only this report's structural, text-free figures are retained anywhere.
