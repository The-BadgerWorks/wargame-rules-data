<!-- AI-Assisted: Claude Code (model: claude-sonnet-5) - Status note for 007 T031, recording why
     the live/cached-corpus run this task names was not performed in this session and exactly how
     to run it. Not the measurement itself — no corpus was read to produce this file. -->
# T031 status: PENDING — live/cached corpus run not yet performed

**The whole-corpus re-derivation tool (`tools/composition_header_refusal_report.py`) is built,
tested, and ready** (12 unit tests passing in
`tests/unit/test_composition_header_refusal_report.py`, including three that assert its `refuses()`
classifier agrees with the shipped production rule,
`pipeline/curate/assemble.py::_refuse_header_row`, on the exact same GF13 (two-row header), GF14
(single-row header), and GF15 (near-miss) fixtures T028's own test
(`tests/enrichment/test_composition_header_refusal.py`) already proves the production rule against
— so this tool is not a second, independently-drifting implementation of research D1's five-signal
rule, it is the same rule re-derived over a wider input). **This file records that T031's own run —
feeding it the live or a legitimately cached current-edition corpus — was not performed in this
session**, and exactly how a session with the right access should perform it.

## Why this session could not run it

T031 is marked **[EXTERNAL ACTION]** in `tasks.md`'s own human-gate table for exactly this reason,
distinct from T004's pre-implementation population probe: T031 re-derives the *actual refusal set*
T030's shipped rule produces, for review before the fix ships (risk R-1/R-A's own mitigation), and
that re-derivation requires the same live/cached current-edition corpus T003/T004 could not reach
in this session either (`reports/footnote-restriction-taxonomy/2026-08-13-STATUS.md`,
`reports/header-refusal-population/2026-08-13-STATUS.md`). Confirmed directly in this session:
running `python tools/composition_header_refusal_report.py` with no `--fixtures`/`--offline` flags
fails with

```
composition_header_refusal_report.py: WGC_DETAIL_SOURCE_URL is not set, so a live acquisition has
no source to read. Set it to the export directory under csv mode, or to the current-edition tree
under html mode (see docs/configuration.md); or run against a fixture set, which never reads it.
```

The same three constraints recorded in T003/T004's identical status notes apply here without
change: this session's operating instructions prohibit initiating any fetch against the live
source; `WGC_DETAIL_SOURCE_URL` is unset in this checkout's environment; and no cached corpus
exists anywhere in this repository to substitute for a live run, by the constitution's own design
(raw source is never committed).

## Exactly how to run it, when access exists

```bash
python tools/composition_header_refusal_report.py
```

No `--fixtures`/`--offline` flags. Produces
`reports/composition-header-refusal/YYYY-MM-DD.md` (note: without the `-STATUS` suffix this file
carries, to distinguish a real measurement from this placeholder) in the format this tool's own
`render()` already emits and this session's smoke test against `fixtures/minimal` already
exercised end to end (the offline smoke corpus carries no phantom-header shape, so the smoke test
proves the code path runs cleanly rather than exercising a populated refusal list).

## What the resulting report answers

Every composition row research D1's full five-signal conjunction refuses, across the **whole**
current-edition corpus, listed by datasheet id, line ordinal, and the row's own (already-published)
model name. Per D8's R-1/R-A: a refusal set of exactly the eight Kill Team datasheets research D1
measured confirms the fix is scoped as measured; a larger set is the risk materialising and is a
finding for human review — each additional row read against its own card — not an automatic defect,
since the conjunction is designed so a false positive requires a first row simultaneously
unlinkable, fixed-size, and numerically equal to the total of every other row.

**This is the review gate `tasks.md` requires before T030's fix ships**, distinct from T004's
pre-implementation population probe: whoever performs T031 for real should read its own report and
resolve every listed row before this feature's Part-A pull request (plan.md Delivery sequencing #1)
is approved.
