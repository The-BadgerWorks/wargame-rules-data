<!-- AI-Assisted: Claude Code (model: claude-sonnet-5) - Status note for 007 T003, recording why
     the live/cached-corpus run this task names was not performed in this session and exactly how
     to run it. Not the measurement itself — no corpus was read to produce this file. -->
# T003 status: PENDING — live/cached corpus run not yet performed

**T002's classifier (`tools/item_constraint_taxonomy.py`) is built, tested, and ready** (17 unit
tests passing against synthetic skeletons and the offline `fixtures/minimal` set — see
`tests/unit/test_item_constraint_taxonomy.py`). **This file records that T003's own run — feeding
it the live or a legitimately cached current-edition corpus — was not performed in this session**,
and exactly how a session with the right access should perform it.

## Why this session could not run it

T003 is marked **[EXTERNAL ACTION]** in `tasks.md`'s own human-gate table for exactly this reason:
it requires either a live acquisition against the configured detail source or an already-cached
corpus, both of which are subject to constraints this session does not have standing to satisfy
on its own:

- The pipeline's own acquisition politeness rules (UA, `robots.txt`, request interval) bind any
  live run, and this session's operating instructions separately and explicitly prohibit
  initiating any fetch against the live source, full stop — not "fetch carefully", **never fetch**.
- `WGC_DETAIL_SOURCE_URL` is unset in this checkout's environment (confirmed: a live invocation
  fails with `ConfigError` before any request is constructed — see
  `test_a_live_run_without_a_source_url_is_a_configuration_error` in this tool's own test file),
  so there is no configured live target to point at even setting the prohibition aside.
- No cached corpus exists anywhere in this repository to run the classifier against **offline**
  instead: raw acquired source is deliberately never committed (the constitution's own IP
  boundary — `work/` is gitignored and emptied on every run, `.gitignore`'s first rule), so
  "cached" here can only mean a live run's own ephemeral acquisition, immediately preceding the
  classification, in a session actually authorised to make it.

## Exactly how to run it, when access exists

```bash
python tools/item_constraint_taxonomy.py
```

No `--fixtures`/`--offline` flags — those select the synthetic rehearsal path this session already
proved. A live run needs a configured `WGC_DETAIL_SOURCE_URL` (and the html-mode
`WGC_DETAIL_ACQUISITION_MODE`/`WGC_DETAIL_EDITION` pair research D1's current-edition requirement
already names — see `pipeline/acquire/detail_source.py`'s module docstring), and produces
`reports/footnote-restriction-taxonomy/YYYY-MM-DD.md` in the format this tool's own `render()`
already emits and this session's smoke test against `fixtures/minimal` already exercised end to
end (zero option/equipment rows in that particular fixture set, so the smoke test proves the code
path runs cleanly rather than exercising a populated report — the live run is what populates it).

## What the resulting report answers

Per-cell counts of (diagnosis class × restriction signal), for every unparsed options-grammar row,
plus the same two signals tested against the composition and equipment grammars' own residuals.
Read per the tool's own "Reading this" section: a `non_summing`-analogue here is a **non-zero**
negation/cardinality count in either residual table, which is the signal that a second footnote-
restriction arrival path exists (risk R-J) and T039's production needs a second site. A report
where every restriction-signalled row lands under the options grammar's own diagnosis classes, and
both residual tables carry only `neither`, is R-J **confirmed**: no second arrival path, and
T036's constraint vocabulary needs no second production site.

**This determination directly sizes T036 (Phase 5, User Story 3) before its vocabulary is fixed.**
Whoever performs T003 for real should read its own report before that task starts, exactly as
`tasks.md` requires.
