{
  "_comment": "AI-Assisted: Claude Code (model: claude-opus-5) - Authored per data-model.md §4 and §3.5 (task T034). restriction_type is drawn ONLY from reference-db-schema.md §4's closed vocabulary at restriction_vocabulary_version 1; an out-of-vocabulary value is the blocking CON-RESTRICTION-VOCAB, never passed through, because the app treats an unknown type as unevaluated rather than satisfied. detachment_id omitted = an edition-wide rule. message_template is AUTHORED, short, and mechanical, and carries no publisher wording (reference-db-schema.md §6).",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://the-badgerworks.github.io/wargame-rules-data/schemas/curation/detachment-restrictions.schema.json",
  "title": "curation/detachment-restrictions.json",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "restriction_type", "params", "message_template"],
    "properties": {
      "id": { "type": "string", "minLength": 1 },
      "detachment_id": { "type": "string", "pattern": "^d-[a-z0-9-]+$" },
      "restriction_type": {
        "enum": [
          "max_copies_per_datasheet",
          "max_units_with_keyword",
          "min_units_with_keyword",
          "requires_keyword_in_army",
          "forbids_keyword_in_army",
          "forbids_datasheet",
          "legends_allowed",
          "max_enhancements",
          "unique_epic_heroes"
        ]
      },
      "params": { "type": "object" },
      "message_template": { "type": "string", "minLength": 1, "maxLength": 240 }
    }
  }
}
