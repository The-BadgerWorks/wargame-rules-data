{
  "_comment": "AI-Assisted: Claude Code (model: claude-opus-5) - Authored per validation-report.md §5 and data-model.md §4 (task T034). A resolution suppresses its finding ONLY while data_digest matches: when the underlying data changes the resolution lapses automatically and the finding is raised again at its catalogue severity, so a curator cannot silence a class of finding permanently (FR-034). Suppressed findings still appear in the report under `suppressed`, with their explanation, so an approver can see what has been waved through and why. `explanation` is a human sentence about mechanics or process and MUST NOT quote source text.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://the-badgerworks.github.io/wargame-rules-data/schemas/curation/resolutions.schema.json",
  "title": "curation/resolutions.json",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "finding_code",
      "entity_ref",
      "data_digest",
      "resolved_at",
      "resolved_by",
      "explanation"
    ],
    "properties": {
      "finding_code": { "type": "string", "pattern": "^[A-Z]{3}-[A-Z-]+$" },
      "entity_ref": { "type": "string", "minLength": 1 },
      "data_digest": { "type": "string", "minLength": 1 },
      "resolved_at": { "type": "string", "minLength": 1 },
      "resolved_by": { "type": "string", "minLength": 1 },
      "explanation": { "type": "string", "minLength": 1, "maxLength": 400 }
    }
  }
}
