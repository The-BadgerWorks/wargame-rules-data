{
  "_comment": "AI-Assisted: Claude Code (model: claude-opus-5) - Authored per data-model.md §4 (task T034). Consulted BEFORE any name matching (research D5 stage 1): a confirmed pairing is never re-derived, so an upstream rename lands as a changed display name on an unchanged curated id -- reported as a rename, never as a removal plus an addition (FR-015).",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://the-badgerworks.github.io/wargame-rules-data/schemas/curation/unit-map.schema.json",
  "title": "curation/unit-map.json",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "datasheet_id",
      "mfm_display_name",
      "wahapedia_datasheet_id",
      "confirmed_at",
      "confirmed_by"
    ],
    "properties": {
      "datasheet_id": { "type": "string", "pattern": "^ds-[a-z0-9-]+$" },
      "mfm_display_name": { "type": "string", "minLength": 1, "maxLength": 120 },
      "wahapedia_datasheet_id": { "type": "string", "minLength": 1 },
      "confirmed_at": { "type": "string", "minLength": 1 },
      "confirmed_by": { "type": "string", "minLength": 1 }
    }
  }
}
