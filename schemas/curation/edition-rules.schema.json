{
  "_comment": "AI-Assisted: Claude Code (model: claude-opus-5) - Authored per data-model.md §4 (task T034). Non-tabular construction rules, authored because no source publishes them in machine-readable form. `value` is any JSON value; the builder serialises it canonically into edition_rule.value_json. Unknown keys are ignored by the consuming app, so adding a rule is additive.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://the-badgerworks.github.io/wargame-rules-data/schemas/curation/edition-rules.schema.json",
  "title": "curation/edition-rules.json",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["rule_key", "value"],
    "properties": {
      "rule_key": { "type": "string", "pattern": "^[a-z0-9_]+$" },
      "value": {}
    }
  }
}
