{
  "_comment": "AI-Assisted: Claude Code (model: claude-opus-5) - Authored per curated-snapshot-format.md §2.2 (task T032). Contiguity, non-overlap, and full coverage of reference-db-schema.md §3.4's declared 500..5000 range are cross-record properties a JSON Schema cannot express; they are validation rule V3 (CON-BAND-GAP / CON-BAND-OVERLAP), and the range is read from that contract rather than from configuration (C7/R9).",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://the-badgerworks.github.io/wargame-rules-data/schemas/curated/game-sizes.schema.json",
  "title": "Curated game-sizes.json",
  "description": "Point bands, sorted by min_points.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "label",
      "min_points",
      "max_points",
      "detachment_points_budget",
      "max_detachments",
      "max_enhancements"
    ],
    "properties": {
      "id": { "type": "string", "minLength": 1 },
      "label": { "type": "string", "minLength": 1 },
      "min_points": { "type": "integer", "minimum": 0 },
      "max_points": { "type": "integer", "minimum": 0 },
      "detachment_points_budget": { "type": "integer", "minimum": 0 },
      "max_detachments": { "type": "integer", "minimum": 0 },
      "max_enhancements": { "type": "integer", "minimum": 0 }
    }
  }
}
